<fieldset id="payment" data-hook>
  <legend><%= t(:payment_information) %></legend>
  <div data-hook="checkout_payment_step">
    <%= form.fields_for :payments, @order.available_payment_options do |p| %>
      <% method = p.object.payment_method %>
      <div>
        <label>
          <%= p.radio_button :payment_method_id, method.id %>
          <%= t method.name, scope: :payment_methods, default: method.name %>
        </label>
        <fieldset>
          <%= render partial: "spree/checkout/payment/#{method.method_type}",
            locals: { payment_method: method, form: p } %>
        </fieldset>
      </div>
    <% end %>
    <br style="clear:both;" />
    <div data-hook="coupon_code_field" data-hook></div>
  </div>
</fieldset>

<br class="space" />

<div class="form-buttons" data-hook="buttons">
  <%= submit_tag t(:save_and_continue), :class => 'continue button primary' %>
  <script>disableSaveOnClick();</script>
</div>
